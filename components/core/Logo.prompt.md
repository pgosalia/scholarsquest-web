The supplied Scholar's Quest artwork; never redraw the mark.

```jsx
<Logo variant="full-color" height={44} assetBase="../.." />
```

Use `full-white` / `mark-white` on ink or teal grounds, `mark-color` for avatars, favicons and tight spaces.
