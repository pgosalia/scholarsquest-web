Renders a Lucide glyph tinted with currentColor; use for every UI icon in Scholar's Quest.

```jsx
<Icon name="graduation-cap" size={24} />
```

Names are Lucide kebab-case. Size defaults to 20. Colour comes from the parent's `color`.
